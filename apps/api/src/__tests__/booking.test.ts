import { describe, expect, it } from 'vitest';
import { calculateFee, generateSlots, isFollowupEligible } from '@medinova/shared';
describe('booking rules',()=>{const fees={consultationFee:1000,followupFee:600,telemedicineFee:800,followupValidDays:14};it('calculates new and telemedicine fees',()=>{expect(calculateFee(fees,'new')).toBe(1000);expect(calculateFee(fees,'telemedicine')).toBe(800)});it('applies follow-up only inside validity window',()=>{const today=new Date().toISOString().slice(0,10);expect(calculateFee(fees,'followup',[{doctorId:'d',date:today,status:'completed'}])).toBe(600);expect(calculateFee(fees,'followup',[{doctorId:'d',date:'2020-01-01',status:'completed'}])).toBe(1000);expect(isFollowupEligible([{doctorId:'d',date:today,status:'completed'}],'d',today,14)).toBe(true)});it('generates non-overlapping bounded slots',()=>{expect(generateSlots('10:00','10:45',20)).toEqual([{start:'10:00',end:'10:20'},{start:'10:20',end:'10:40'},{start:'10:40',end:'10:45'}]);expect(generateSlots('10:00','10:10',0)).toEqual([])})});
